import React, { useState, useMemo } from 'react';
import ReactFlow, { addEdge, MiniMap, Controls, Background, Handle } from 'react-flow-renderer';
import { Box } from '@mui/material';
import 'react-flow-renderer/dist/style.css'; // core styles
import 'react-flow-renderer/dist/theme-default.css'; // default theme


const NetworkDiagram = ()=>{
    return (
        <Box style={{height: "100vh"}}>
            Hello
        </Box>
    );

}

export default NetworkDiagram;
